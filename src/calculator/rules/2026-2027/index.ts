import type { UnconfiguredTaxRuleConfig } from "../types";

/**
 * Assessment Year 2026-2027 tax rules.
 *
 * NOT YET CONFIGURED. Do not fill this in with assumed, remembered, or
 * estimated figures — official Bangladesh tax slabs, exemptions,
 * rebate rules, surcharge thresholds, and minimum tax figures for this
 * assessment year must be supplied before this file is implemented.
 *
 * Once supplied, this file should export a `TaxRuleConfig` object
 * (see ../types.ts) instead of `null`, with a `source` field citing
 * exactly where the figures came from.
 */
const rules2026_2027: UnconfiguredTaxRuleConfig = null;

export default rules2026_2027;
